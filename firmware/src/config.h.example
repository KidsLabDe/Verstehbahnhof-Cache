#pragma once

// Kopiere diese Datei nach config.h und trage echte Werte ein.
// config.h ist in .gitignore und wird nicht eingecheckt.

// --- WLAN ---
#define WIFI_SSID       "DEIN_WLAN"
#define WIFI_PASSWORD   "DEIN_PASSWORT"

// --- API (MatzE) ---
// Liefert den aktuellen Zustand der Reise als JSON, z.B.:
//   { "state": 2, "station": "repair_cafe", "last_activity": "…" }
// Traefik liefert /state zusätzlich plain über http aus (siehe
// docker-compose.yml), damit wir auf dem ESP8266 kein BearSSL brauchen.
#define API_URL         "http://verstehbahnhof.kidslab.de/state"
// Die Firmware pollt nicht mehr zeitgesteuert: der API-Call passiert
// nur dann, wenn der Zug am nächsten (roten) Bahnhof angekommen ist.
// Der HTTP-Timeout sollte trotzdem kurz sein, sonst "hängt" der Zug am
// Wendepunkt sichtbar bis zur Antwort.
#define API_TIMEOUT_MS  1500

// --- NeoPixel ---
// D2 am Wemos D1 Mini == GPIO4 (sicherer Pin, keine Boot-Konflikte)
#define NEOPIXEL_PIN    D2
#define NEOPIXEL_COUNT  64   // LEDs auf dem Streifen insgesamt
#define NEOPIXEL_BRIGHTNESS 80  // 0..255

// LED-Positionen der Bahnhöfe auf dem Streifen (0-basiert).
// Reihenfolge: Fürstenberg → Berlin → Leipzig → Nürnberg → Augsburg.
// Anzahl muss mit STATION_NAMES[] in main.cpp übereinstimmen (5 Einträge).
static const uint16_t STATION_LEDS[] = { 0, 14, 31, 48, 63 };

// Wie schnell der Zug zwischen zwei Bahnhöfen pendelt (ms pro LED-Schritt).
// Kleinere Zahl = schneller. 18 ≈ 55 fps.
#define TRAIN_FRAME_MS  90

// Anzahl Waggons hinter der Lok (dunkler werdendes Blau)
#define NUM_WAGONS      3

// --- Attract-Animation (Feuer + Feuerwerk, wenn noch niemand den
//     Initial-QR gescannt hat, d.h. state < 0) ---
#define ATTRACT_POLL_MS    10000  // wie oft während Attract die API fragen
#define FIRE_COOLING       55     // Fire2012 Abkühlrate (höher = dunkler)
#define FIRE_SPARKING      120    // Zündwahrscheinlichkeit 0..255
#define MAX_BURSTS         6      // gleichzeitige Feuerwerks-Bursts
#define BURST_FADE         12     // Helligkeitsabfall pro Frame 0..255
#define BURST_SPAWN_CHANCE 8      // Promille pro Frame neuer Burst

// Die Bahnhofsnamen stehen in main.cpp in STATION_NAMES[]. Werden nur
// fürs Serial-Log benutzt.
